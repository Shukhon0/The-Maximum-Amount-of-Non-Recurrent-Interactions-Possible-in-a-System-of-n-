var number = (6);
// don't shake hands during a pandemic lmao
// no really don't
function maxint () {
    return number*(number-1)/2; 
}
document.getElementById('nth').textContent=(number);
document.getElementById('sum').textContent=(maxint());
